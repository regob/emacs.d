;;; init-dockerfile.el --- Dockerfile TS mode -*- lexical-binding: t -*-
;;; Commentary:
;;; Code:

(if-treesit
    (use-package dockerfile-ts-mode
      :ensure nil
      ))


(provide 'init-dockerfile)

;;; init-dockerfile.el ends here
