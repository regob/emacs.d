;;; init-format-all.el --- Format-all multi-language formatter -*- lexical-binding: t -*-
;;; Commentary:
;;; Code:

(when-home
    (use-package format-all
      :ensure t
      :pin melpa))

(provide 'init-format-all)

;;; init-format-all.el ends here
